# MODEL.md · 模型说明

## 1. 它是什么 / 不是什么

**是**：一个**储备池式（Reservoir Computing）**的“本能”信号源——一个**权重冻结、不学习**的递归网络，把输入刺激打散成**高维、结构化、非平凡**的“本能反应”。

**不是**：
- 不是智能体、不学习、没有记忆、没有意识。
- 不是“上传的果蝇大脑”。它用的是果蝇大脑的**静态线路图（连接组）**，不记录动态放电。
- 网络上那些“玩 Doom / 开车”的 viral 效果，本质是“真实线路图 + 开发者叠上去的简化动力学与动作映射”，**行为多为设计出来的**。

**卖点**：比白噪声“有结构”，比脚本规则“有层次”——落在“活的、非学习的反应”这个区间。

---

## 2. 架构

```
  stimulus(t)                冻结递归核心                 冻结读出
  ┌─────────┐   Win   ┌───────────────────────┐   Wout   ┌──────────┐
  │ 刺激输入 │ ───────▶ │  W[post,pre]  (真实   │ ───────▶ │ reaction │
  │ (1 通道) │ (兴奋    │  连接组·带符号·冻结)   │ (随机     │  (0~1)   │
  └─────────┘  投影)   │  + LIF 动力学         │  投影)    └──────────┘
                       └───────────────────────┘
                                ↑ 递归（sp_prev -> sp）
```

三个部分，**全部冻结、零训练**：

1. **递归核心 `W`**（真实数据）
   - 来自 MaleCNS 真实连接组子图，稀疏 CSR，`W[post, pre]`。
   - 每个突触按**突触前神经元的递质**赋符号：GABA/glycine → 抑制(−)，其余 → 兴奋(+)。
   - 按**谱半径归一**到临界点附近（本包 raw 谱半径 ~2091 → 归一到 0.9），保证递归有丰富而不爆炸的动力学。
2. **输入接口 `Win`**（冻结随机投影）
   - 把 1 通道刺激投到**顶层 out-degree 神经元**（默认 800 个）作**兴奋驱动**。
   - 因该子集未标注明确的“感受神经元”，用冻结投影是**诚实且非学习**的做法。
3. **读出接口 `Wout`**（冻结随机投影）
   - 全部神经元活动 → 单一反应标量。同样冻结、不训练。

**动力学**：Leaky Integrate-and-Fire（LIF）。每步
`v += (dt/τ)·(0−v) + dt·(I·gain)`，`I = W·sp_prev + Win·stimulus`，`v≥v_th` 即发放并重置。
给定 `seed` + 刺激，输出**确定性**；`noise>0` 可叠加热噪声（可选，更接近真实生物）。

---

## 3. 数据出处（Provenance）

| 项 | 值 |
|---|---|
| 底层数据 | **MaleCNS v1.0**（成年雄性果蝇中枢神经连接组），Janelia FlyEM |
| 许可 | **CC-BY 4.0（需署名）** |
| 官方源 | <https://male-cns.janelia.org/download/> |
| 本包子集 | `nosuchstudios/fruit-fly-brain-runtime`（HuggingFace，CC-BY 4.0，非受限） |
| 子集规模 | **载入 10,000 节点 / 1,587,930 条边**（源 PROVENANCE 标注 ~9,999 节点） |
| 子集选法 | 从确定性种子集按 **CX / Kenyon_Cell / MBON / descending** 类 best-first 扩张（max_neurons=10000, seed=0） |
| 抑制占比 | ~11.9%（GABA/glycine 神经元） |
| 权重含义 | **突触计数**（minconf-0.5），非带符号有效突触权重 |
| ID 命名空间 | **MaleCNS body ID**（≠ FlyWire root ID；**跨数据集勿做 ID join/映射**） |

**完整性校验**（sha256，与源 PROVENANCE 一致）：
- `edges.csv` `189378…f94a08f`
- `annotations.csv` `b77d34…725a`
- `neurotransmitters.csv` `1a566c…a17693e`

---

## 4. 完整版接入（1.05 GB）

- 文件：`connectome-weights-male-cns-v1.0-minconf-0.5.feather`
- 规模：**16.67 万神经元 / 2560 万条连接**（校对过的完整矩阵）
- 官方直链：
  `https://storage.googleapis.com/flyem-male-cns/v1.0/connectome-data/flat-connectome/connectome-weights-male-cns-v1.0-minconf-0.5.feather`
- 接入方法见 `README.md`「接入完整版 MaleCNS」：下载 → `pandas.read_feather` 转 CSV → 同一 `from_malecns()` 接口加载。
- 代价：稀疏矩阵 ~2560 万非零元，内存数百 MB，计算量约为子集 ~16 倍。

> 另有两个大文件**与本引擎无关，不需要**：
> - `syn-points`（~12.7 GB）：每个突触在 EM 体积里的 **3D 坐标**，仅用于 3D 可视化/共定位。
> - `syn-partners`（~6.8 GB）：每个突触上的**共贴神经元（伙伴/共激活）**，仅用于伙伴分析。
> 本能引擎只需“谁连谁”（权重），不需要空间位置。

---

## 5. 诚实边界（务必遵守）

1. **递质符号是先验**，不是实验测定：GABA/glycine 判抑制是“预测（a prior）”，非 ground truth。
2. **权重是突触计数**，非带符号有效权重；符号是后加的先验。
3. **不宣称**任何“生物学优势 / 通用记忆 / AGI / 编程专长 / 意识”。
4. 输入/输出接口是**冻结随机投影**（非标注的生理感受/运动神经元）——这是工程近似，不是生理事实。
5. 输出是**确定性**的（除非加噪声）；“随机感/本能感”来自**结构**，不是随机源。
